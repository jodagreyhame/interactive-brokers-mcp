# Gateway Configuration
IB_GATEWAY_HOST=localhost
IB_GATEWAY_PORT=5000

# MCP Server Configuration
MCP_SERVER_PORT=3000
NODE_ENV=production
LOG_LEVEL=info

# Optional Configuration
TZ=UTC

# Authentication Note:
# The IB Gateway uses OAuth/session-based authentication through the web interface
# No credentials need to be stored in environment variables
